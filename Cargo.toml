[package]
name = "ib"
version = "0.1.0"
edition = "2021"
description = "Rust and Oracle simulation trading platform for paper trading and strategy development"

[dependencies]
oracle = "0.6"
chrono = "0.4"
rust_decimal = { version = "1", default-features = false, features = ["std"] }

[profile.release]
strip = true
