# 项目待办与执行记录

更新时间：2026-08-25

## 当前结论

项目已经具备 Oracle 数据库连接、模拟交易账本、用户注册登录、基础前端、Caddy 和 systemd 部署能力。当前前端原先只覆盖认证模块；本轮优先补齐认证用户到模拟交易的最小闭环。

## 本轮执行

- [x] 确认数据库连接、wallet 和 systemd 服务正常。
- [x] 初始化 `USERS`、`SESSIONS` 认证表。
- [x] 预留固定密码的测试用户。
- [x] 为登录用户自动绑定一个模拟账户。
- [x] 增加账户、合约、订单、撤单、模拟成交、持仓和现金的 Web API。
- [x] 将交易数据接入前端 Dashboard。
- [x] 增加交易 API 的输入校验和基础测试。
- [x] 将内嵌原生前端迁移到 Vite + React + TypeScript。

## 外部依赖待办

- [ ] Resend 邮箱验证：需要真实 `RESEND_API_KEY`、发件域名和验证邮件模板；当前注册允许直接登录，`email_verified` 保持为 `false`。
- [ ] 行情驱动撮合：需要行情源或回测时钟，当前仍由 `fill add` 注入成交。
- [ ] 手续费、保证金和风控：需要明确模拟规则后实现。

## 后续增强

- [ ] 账户资产净值、未实现盈亏和行情快照。
- [ ] 分页、筛选、订单详情和成交明细。
- [ ] 管理员与普通用户权限分离。
- [ ] 数据库迁移版本表和可重复执行迁移。
- [ ] 集成测试：Oracle 测试库、登录态、订单到成交账务闭环。

## 验收标准

1. 登录用户只能看到并操作自己的模拟账户。
2. 前端可以查看账户、合约、订单、持仓和现金。
3. 前端可以提交订单、撤单，并注入模拟成交。
4. 订单和账本更新继续复用现有事务逻辑。
5. 未配置 Resend 时不伪造“邮箱已验证”。
